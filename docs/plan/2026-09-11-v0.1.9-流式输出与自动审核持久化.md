# v0.1.9 流式输出中断与自动审核持久化

## 背景

1. 命令事件插入对话后，SSE `chunk` 只追加「最后一条」消息；最后一条变成 `command` 后，助手正文被丢弃，需刷新才从 DB 看到完整回复。
2. AI 自动审核仅存进程内存 + 前端模块态，刷新页面丢失。

## 修复

1. chunk 追加到「末尾连续的 assistant」；若末尾是 command，新开 assistant 气泡；结束后 force 同步消息。
2. `managed_agents.auto_review` 落库；开关时写入；列表带回；加载会话时恢复 Hub + UI。
