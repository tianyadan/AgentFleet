# v0.2.17 Design

**Claude early stop:** `PlanConcreteSteps` → `runManagedEngine(..., noHook=true)` still resumed/saved `engine_session_id` on the agent’s main conversation. Fix: `noHook` ⇒ no resume + no `RememberEngineMeta`. Also ship `db/migrate_v0.2.17.sql` to clear already-polluted Claude sessions once.

**Codex timeline:** Prefix each `agent_message` chunk with `\n· `; on `command_execution` emit SSE `command` + persist `messages.role=command` (same JSON shape as Claude audits). UI: gray · timeline for commands.
