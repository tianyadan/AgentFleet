# Design: v0.1.10 E-bot home UX

Approved via user request to document and implement.

1. Replace top nav label「提问」with slogan「多智协同，分身执行」(small text).
2. Cache per-mode `{convId, chatLog}`; switchMode saves/restores so logs do not cross modes.
3. Single mode: after assistant `done`, append divider tip message.
4. Remove「开始新对话」link; keep「＋ 新建对话」only.
