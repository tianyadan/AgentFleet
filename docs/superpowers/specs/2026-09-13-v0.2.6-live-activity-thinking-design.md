# v0.2.6 Design: live activity + thinking + images

See `docs/plan/2026-09-13-v0.2.6-运行态工具状态与对话体验.md`.

## SSE

- `type: "activity"` → `{ tool, summary }`
- Claude: parse stream-json `tool_use` via `ParseStreamActivity`
- Codex/Cursor: coarse “执行中…”
- Permission `command` also emits a short activity

## Frontend

- No empty assistant bubble on send
- Silver gradient rotating thinking phrases until / while running
- Activity line under thinking
- Assistant: flat (no bubble) + copy button
- `AgentMarkdown` + `GET /api/admin/agents/:id/file?path=` for workspace images
