# v0.1.8 任务500 / 终止 / Bark-A / 命令审计 / UI

## 背景

1. `GET /api/admin/agents/:id/tasks` 500：库缺 `agent_tasks`。
2. 运行中无法真正终止对话。
3. Bark 应仅在用户不看该 agent 时推送，且文案含智能体名/命令/风险。
4. 拒绝的命令也要展示并持久化审批元数据。
5. 智能体详情页布局需更干净。

## 目标

- tasks 可用或空列表；可终止；Bark 策略 A；命令审计入库；UI 整理。

## 范围

见 design spec。不改 Claude hook 沙箱核心逻辑。

## 开发 TODO

- [x] migrate agent_tasks + command_audits
- [x] stop API + cancel Ask
- [x] Bark 改前端触发 + 文案
- [x] 审计落库 + 前端展示
- [x] ManagedAgents UI
- [x] changelog v0.1.8
