# v0.2.12 Workflow Interruption Recovery Implementation Plan

> **For agentic workers:** Implement task-by-task with TDD on SelfCheck/PlanResume.

**Goal:** 同进程适配的 waiting_recovery + 3 分钟自动自检续跑 + 手动恢复工作流。

**Architecture:** 启动标记 waiting_recovery；Resume 复用可自检 success 节点；从 needRerun roots 多起点推进。

**Tech Stack:** Go/Gin, React JSX

## Global Constraints

- 版本 v0.2.12；不拆 Worker；不改 AskManaged/权限/交接/记忆

---

### Task 1: SelfCheck + PlanResume

**Files:** Create `server/internal/service/workflow_recover.go` + `_test.go`

### Task 2: Store + Startup window + ResumeWorkflowRun

**Files:** `store/workflow.go`, `service.go`, `workflow_engine.go`, handler route

### Task 3: Frontend + changelog

**Files:** WorkflowRun.jsx, workflowApi.js, changelog, status labels
