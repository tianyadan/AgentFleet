# Design: v0.1.8 tasks/stop/Bark-A/command-audit/UI

Approved 2026-09-11 (Bark strategy A).

## Decisions

1. Create `agent_tasks` via migrate_v0.1.8; tasks API degrades to empty list on missing table.
2. Running UI: 插话 + 终止; `POST /admin/agents/:id/stop` cancels Ask context.
3. Bark only when frontend detects user not viewing that agent; rich message with name/command/risk.
4. Table `command_audits` for allow/deny with by=user|ai|system; show denied in chat UI.
5. Clean up ManagedAgents detail layout CSS/structure without Ant Design rewrite.
