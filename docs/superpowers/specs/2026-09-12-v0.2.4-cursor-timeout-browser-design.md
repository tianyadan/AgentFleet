# v0.2.4 Design

## Root causes

1. **Invalid CURSOR_API_KEY**: Server inherits Cursor Agent session env; `GenericAsk`/`AskStream` pass it to `agent` CLI. User deleted shell profile key but live server process still has it.  
2. **signal: killed @ ~15min**: `AVATAR_TIMEOUT_SEC` default 900 → `context.WithTimeout` SIGKILLs child.  
3. **No recover**: First kill ends AskManaged without resume.  
4–6. Product gaps as in plan.

## Fixes

See `docs/plan/2026-09-12-v0.2.4-Cursor密钥超时自检与浏览器权限.md`.
