# v0.2.7 Implementation Plan

> **For agentic workers:** Implement task-by-task. Inline execution approved by user.

**Goal:** Team workflow orchestration with agent occupancy and long-term memory.

**Architecture:** Domain-split workflow modules; process-local DAG; reuse AskManaged; React Flow UI.

**Tech Stack:** Go/Gin/MySQL, React + `@xyflow/react`

## Global Constraints
- No second Agent Runtime; reference managed_agents only
- Condition: expression only (`ai_enabled` reserved)
- Restart incomplete runs → `interrupted`
- Occupancy table: `agent_occupancy`
- Memory via MemoryService Top 3–5
- Do not grow ManagedAgents.jsx with Canvas

## Tasks
1. DB migrate + schema
2. Store workflow / occupancy / memory
3. Expr + Occupancy + Memory services (+ tests)
4. Workflow CRUD + DAG engine
5. Handlers + routes + AskManaged/Schedule wire
6. Frontend workflow module + App + busy banner
7. Changelog, go test, lint, run migrate SQL
