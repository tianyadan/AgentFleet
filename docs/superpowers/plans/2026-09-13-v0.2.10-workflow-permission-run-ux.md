# v0.2.10 团队编排权限与运行态 UX Implementation Plan

> **For agentic workers:** Implement task-by-task. Prefer TDD on Go handler/store helpers.

**Goal:** 修复团队编排授权无弹窗，并完成自动审核开关、菜单徽标/改名、终止任务可回看、节点红绿框与自动滚动。

**Architecture:** 管理端 JWT 可见 pending；按 running `node_executions.conversation_id` 聚合工作流待授权；前端 Overlay 复用风险卡；节点 `auto_review` 写入会话 SetAuto；菜单与运行页纯前端 UX。

**Tech Stack:** Go/Gin, React JSX, 现有 permission Hub / Reviewer

## Global Constraints

- 版本：v0.2.10
- 不改动敏感命令分类核心规则（复用 Classify + AI Reviewer）
- 按钮/菜单文案：团队编排（不用「任务计划」）

---

### Task 1: Pending 对管理员可见 + 工作流 pending API

**Files:**
- Modify: `server/internal/handler/handler.go` (PermissionPending)
- Modify: `server/internal/store/workflow.go` (ListRunningNodeConversationIDs)
- Modify: `server/internal/handler/workflow.go` + routes
- Test: `server/internal/handler/permission_pending_test.go`（或扩展现有）

- [ ] Admin JWT 时 pending 不过滤 IP
- [ ] `GET /api/admin/workflows/permission-pending` → `{ items: [...] }`
- [ ] `go test ./internal/handler/... ./internal/store/...`

### Task 2: 节点 auto_review

**Files:**
- Modify: `server/internal/service/workflow_graph.go` (DataBool)
- Modify: `server/internal/service/workflow_engine.go` (runAgent SetAuto)
- Modify: `web/src/workflow/NodePanel.jsx`
- Test: DataBool unit test

### Task 3: PermissionCard + Workflow overlay + 菜单徽标

**Files:**
- Create: `web/src/PermissionCard.jsx`
- Create: `web/src/workflow/WorkflowPermOverlay.jsx`
- Modify: `web/src/App.jsx`, `ManagedAgents.jsx`, `NodeRunPanel.jsx`, `changelog.js`

### Task 4: 列表入口 + 运行页红绿框 + 自动滚动 + 状态标签

**Files:**
- Modify: `WorkflowList.jsx`, `WorkflowRun.jsx`, `WfNodes.jsx`, `App.css`, `NodeRunPanel.jsx`

### Task 5: 文档勾选 + 验证

- [ ] 更新 plan TODO
- [ ] `go test ./...` + `npm run lint`
- [ ] changelog v0.2.10
