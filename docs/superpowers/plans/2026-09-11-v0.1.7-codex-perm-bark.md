# v0.1.7 Codex / 待授权徽标 / Bark Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix Codex managed agents via `codex exec`, show pending-permission badges on the agents menu, and Bark-notify on each new permission request (strategy A).

**Architecture:** Add engine-specific `CodexAsk`; thin `scripts/notify-permission.sh` wrapping `~/bin/ai-notify.sh`; service fires notify after `Register` with request-id dedupe; frontend reads module-level `allPendingPerms()`.

**Tech Stack:** Go, React JSX, Bash + existing Bark script.

## Global Constraints

- Bark strategy A: notify once per new request_id; never block permission Wait.
- Do not use `codex -p` as prompt (profile flag).
- Conventional commit tip: `feat(agents): ...` Chinese body.

---

### Task 1: CodexAsk

**Files:**
- Create: `server/internal/agent/codex.go`
- Test: `server/internal/agent/codex_test.go`
- Modify: `server/internal/service/managed_agents.go`

- [ ] Write `CodexArgs` builder test expecting `exec`, `--skip-git-repo-check`, `-s workspace-write`, optional `-C`
- [ ] Implement `CodexAsk` / `CodexArgs`
- [ ] Route `case "codex":` in `AskManaged`

### Task 2: Bark notify

**Files:**
- Create: `scripts/notify-permission.sh`
- Create: `server/internal/notify/bark.go` (+ test)
- Modify: `server/config/config.go`, `server/internal/service/service.go`

- [ ] Script wraps `ai-notify.sh` with warning
- [ ] `NotifyPermissionOnce(requestID, message)` with dedupe map
- [ ] Call after Register on human-wait path; config `NotifyBin` / `BarkNotify`

### Task 3: Frontend badges + changelog

**Files:**
- Modify: `web/src/App.jsx`, `web/src/ManagedAgents.jsx`, `web/src/App.css`, `web/src/changelog.js`

- [ ] Side menu pending count badge
- [ ] Per-agent list badge
- [ ] Changelog v0.1.7
