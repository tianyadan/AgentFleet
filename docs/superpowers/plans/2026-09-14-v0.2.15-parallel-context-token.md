# v0.2.15 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans (or implement in-session). Steps use TDD where practical.

**Goal:** Fix parallel false-busy; folder run badges; real engine context for Status + ring; per-send token display.

**Architecture:** Dedupe workflow waves; persist CLI session on Ask; shared context fetch API used by Status button and 5s ring poll; SSE usage for live token counter.

**Tech Stack:** Go/Gin, React JSX, MySQL, Claude/Codex/Cursor CLIs.

---

### Task 1: Parallel queue dedupe

**Files:** `server/internal/service/workflow_engine.go`, add/extend test in `workflow_*_test.go`

- Ready list: at most one GraphNode per id per wave
- When appending successors: skip if already in queue or visitedOK
- Test diamond / fan-in enqueues Next once

### Task 2: Session + usage persistence + context API

**Files:** migrate SQL, store, agent stream parsers, `service/agent_status.go` (extend), handler route

- Save session_id + last usage/context_window on conversation or managed_agent
- `GET /admin/agents/:id/context` returns `{ used_tokens, window_tokens, session_id, source, text }`
- Status button uses this; ring uses same

### Task 3: Frontend badges / token / ring

**Files:** `ManagedAgents.jsx`, `App.css`, `changelog.js`

- Folder green/purple mini badges
- RunStatus shows task token total
- Composer ring; poll context every 5s; refresh on ask start/end

### Task 4: Changelog + verify

- `go test ./...` relevant packages; `npm run build`
